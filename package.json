{
  "name": "backyardconnect",
  "version": "1.2.0",
  "private": true,
  "description": "Production connectivity enquiry platform for BackyardConnect.co.za",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "dev": "NODE_ENV=development node --watch server.mjs",
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs && npm test",
    "hash-password": "node scripts/hash-password.mjs",
    "generate-secrets": "node scripts/generate-secrets.mjs"
  }
}
